const Discord = require("discord.js");
const { colorBlack } = require("../../config.js");

module.exports.antiLangWarnUser = function(client, message) {
  // Warn user
};

module.exports.antiLangMuteUser = function(client, message) {
  // Mute user
};

module.exports.antiLangKickUser = function(client, message) {
  // Kick user
};

module.exports.antiLangBanUser = function(client, message) {
  // Ban user
};
