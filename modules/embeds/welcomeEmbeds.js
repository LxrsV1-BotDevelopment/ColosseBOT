const Discord = require("discord.js");
const Canvas = require("canvas");

module.exports = function(client, member, guildData) {
  const canvas = Canvas.createCanvas(700, 250);
  const ctx = canvas.getContext('2d');

  //Not ready yet
};
